const CONFIG={whatsappNumber:"",shopUrl:"https://flpil.co.il/?agent=64213"};const header=document.querySelector(".header"),hero=document.querySelector(".hero"),heroBg=document.querySelector(".hero-bg"),mobileBar=document.querySelector(".mobile-bar"),reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target)}})},{threshold:.14});document.querySelectorAll(".reveal,.step").forEach(el=>observer.observe(el));function onScroll(){const y=scrollY;header.classList.toggle("scrolled",y>24);if(mobileBar){const guidance=document.querySelector("#guidance");mobileBar.classList.toggle("show",innerWidth<701&&y>hero.offsetHeight*.62&&guidance.getBoundingClientRect().top>innerHeight*.35)}if(!reduced&&heroBg){heroBg.style.transform=`translate3d(0,${Math.min(y,hero.offsetHeight)*.07}px,0) scale(1.04)`}}addEventListener("scroll",onScroll,{passive:true});addEventListener("resize",onScroll);onScroll();function waUrl(text){return CONFIG.whatsappNumber?`https://wa.me/${CONFIG.whatsappNumber}?text=${encodeURIComponent(text)}`:`https://wa.me/?text=${encodeURIComponent(text)}`}document.querySelectorAll("[data-whatsapp]").forEach(link=>link.addEventListener("click",event=>{event.preventDefault();location.href=waUrl("שלום שלי, הגעתי דרך האתר ואשמח לקבל ליווי אישי.")}));const form=document.querySelector("#guideForm"),status=document.querySelector("#status");if(form)form.addEventListener("submit",event=>{event.preventDefault();const data=new FormData(form),name=String(data.get("name")||"").trim(),phone=String(data.get("phone")||"").trim(),interest=String(data.get("interest")||"").trim(),time=String(data.get("time")||"").trim(),note=String(data.get("note")||"").trim();if(!name||!phone||!interest){status.textContent="נא למלא שם, טלפון ותחום עניין.";return}status.textContent="ההודעה מוכנה — עוברים ל־WhatsApp.";const text=`שלום שלי, אשמח לקבל ליווי אישי דרך האתר.\n\nשם: ${name}\nטלפון: ${phone}\nתחום עניין: ${interest}\nזמן נוח לחזרה: ${time||"לא צוין"}\nהערה: ${note||"לא נכתבה הערה"}`;setTimeout(()=>location.href=waUrl(text),350)});